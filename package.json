{
  "name": "download-artifact",
  "version": "1.0.0",
  "description": "Download an Actions Artifact from a workflow run",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build src/download-artifact.ts --target es2024",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/artifact": "^4.0.0",
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.0.0"
  }
}
